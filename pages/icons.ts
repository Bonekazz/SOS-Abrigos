import { createIcons, ChevronLeft, X, Bell, BookOpenText, House, MapPinned } from 'lucide';

createIcons({
  icons: {
      BookOpenText,
      House,
      MapPinned,
      Bell, X, ChevronLeft
  }
});
